#!/usr/bin/env bash

export SYNAESTHESIA_HOME="${SYNAESTHESIA_HOME:-$(npm -g root)/synaesthesia}"
export NODE_PATH="${SYNAESTHESIA_HOME}/node_modules"

exec "${NODE_PATH}/lumo-cljs/bin/lumo" -c "${SYNAESTHESIA_HOME}/src" -m syna.esth.esia "$@"
